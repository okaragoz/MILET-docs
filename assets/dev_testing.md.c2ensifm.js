import{_ as t,o as s,c as o,an as i}from"./chunks/framework.Xu3Q0CJC.js";const u=JSON.parse('{"title":"Testing","description":"","frontmatter":{},"headers":[],"relativePath":"dev/testing.md","filePath":"dev/testing.md","lastUpdated":null}'),a={name:"dev/testing.md"};function n(d,e,c,r,l,h){return s(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Testing" tabindex="-1">Testing <a class="header-anchor" href="#Testing" aria-label="Permalink to &quot;Testing {#Testing}&quot;">​</a></h1><h2 id="Running-the-suite" tabindex="-1">Running the suite <a class="header-anchor" href="#Running-the-suite" aria-label="Permalink to &quot;Running the suite {#Running-the-suite}&quot;">​</a></h2><p>The test runner is a plain script, not a <code>Pkg.test</code> target:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/runtests.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # smoke tests only</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RUN_BENCHMARKS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/runtests.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # smoke + short benchmarks</span></span></code></pre></div><p><code>test/runtests.jl</code> replicates the <code>main.jl</code> include list (so <code>MPI.Init()</code> is called exactly once, guarded by <code>MPI.Initialized()</code>), defines <code>const ROOT = abspath(joinpath(@__DIR__, &quot;..&quot;))</code>, and then <code>include</code>s the smoke files. Everything runs in <strong>one Julia process and one namespace</strong>: later test files can use helpers defined by earlier ones (e.g. the <code>_test_rock</code> keyword constructor from <code>test/smoke/test_rheology.jl</code> is used by <code>test_grainsize.jl</code> and <code>test_softening.jl</code>). Threads are optional but exercise the production marker kernels: <code>julia --project=. -t 4 test/runtests.jl</code>.</p><p>Tests that run full cases write to <code>output/...</code> directories; these are scratch artifacts, safe to delete.</p><h2 id="What-each-testset-covers" tabindex="-1">What each testset covers <a class="header-anchor" href="#What-each-testset-covers" aria-label="Permalink to &quot;What each testset covers {#What-each-testset-covers}&quot;">​</a></h2><h3 id="Smoke-tests-always-run" tabindex="-1">Smoke tests (always run) <a class="header-anchor" href="#Smoke-tests-always-run" aria-label="Permalink to &quot;Smoke tests (always run) {#Smoke-tests-always-run}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">File</th><th style="text-align:right;">Testsets</th><th style="text-align:right;">Covers</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test/smoke/test_rheology.jl</code></td><td style="text-align:right;"><em>Rheology pointwise</em>, <em>Density</em></td><td style="text-align:right;"><code>compute_viscosity</code> (constant-Newtonian identity, Arrhenius ∂η/∂T &lt; 0, Drucker–Prager yield cap, strain-weakening ramp monotonicity), <code>compute_VEP_viscosity</code> (0 &lt; Z ≤ 1, χ + Z = 1, η_VEP ≤ η_viscous), <code>compute_density</code> (reference value, expansivity sign, linear <code>phase_drho</code> stacking). Also defines <code>_test_rock</code>.</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_mesh.jl</code></td><td style="text-align:right;"><em>Mesh — 2D Cartesian + tags</em>, <em>Mesh — 3D Cartesian (synthetic config)</em></td><td style="text-align:right;"><code>build_model</code> cell counts and <code>cell_dx</code>/<code>cell_dy</code> against <code>cfg.xsize/(nx-1)</code>, 3-D construction via <code>clone_config(...; dim = 3, ...)</code>.</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_fespaces.jl</code></td><td style="text-align:right;"><em>FE spaces — Taylor-Hood Q2-Q1 + Q1 T</em></td><td style="text-align:right;"><code>build_spaces</code> DOF sanity (velocity &gt; pressure DOFs, temperature space nonempty) and that <code>State(mb, fes, cfg)</code> constructs with matching <code>n_cells</code>.</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_phase_melt.jl</code></td><td style="text-align:right;"><em>Phase change: depth-triggered</em>, <em>Melting: Katz-lite solidus/liquidus</em></td><td style="text-align:right;"><code>apply_phase_change!</code> crossing logic (rock-type switch + Δρ at the 410 km trigger, no-op above it), <code>melt_fraction</code> endpoints and mid-range bracket.</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_grainsize.jl</code></td><td style="text-align:right;"><em>growth-only analytic</em>, <em>wattmeter steady state (p = 3)</em>, <em>composite rheology + regression</em>, <em>mechanism partition consistency</em></td><td style="text-align:right;"><code>update_grain_size</code> against the closed-form growth ODE, convergence of the Austin–Evans wattmeter to <code>grain_size_steady_state</code> from above and below, harmonic diffusion+dislocation composite vs the single-mechanism regression, <code>viscous_mechanism_partition</code> consistency (see <code>doc/design/grain_size.md</code> and <a href="./../physics/grain_size">Grain-size evolution</a>).</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_softening.jl</code></td><td style="text-align:right;"><em>healing decay</em>, <em>viscous strain softening ramp</em>, <em>configurable yield cap</em>, <em>yielded flag</em></td><td style="text-align:right;">Arrhenius healing timescale, <code>visc_soft_factor</code> ramp endpoints, <code>yield_stress_max</code> capping at depth, and the yield flag returned by <code>compute_VEP_viscosity</code> (see <code>doc/design/softening_healing.md</code> and <a href="./../physics/softening">Softening &amp; healing</a>).</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_gmg.jl</code></td><td style="text-align:right;"><em>GMG V-cycle: variable-viscosity Q2 vector Laplacian</em>, <em>GMG V-cycle: 3D Q2 hierarchy</em>, <em>GMG Stokes: matches direct solve</em></td><td style="text-align:right;"><code>build_gmg_hierarchy</code> level/transfer shapes, 8-cycle residual contraction (&lt; 0.5 mean rate) on a 4-decade viscosity contrast, <code>gmg_update!</code> value refresh on a fixed pattern, 3-D hierarchy construction, and FGMRES+GMG vs MUMPS velocity agreement (rel. diff &lt; 2e-3) over a 2-iteration Picard solve — <code>n_picard = 2</code> deliberately exercises <code>assemble_stokes_cached!</code>, the MUMPS numeric-only refactorization, and <code>gmg_update!</code>, the paths production runs live on (see <a href="./../numerics/multigrid">Geometric multigrid</a>).</td></tr><tr><td style="text-align:right;"><code>test/smoke/test_twophase.jl</code></td><td style="text-align:right;"><em>Two-phase: closures</em>, <em>Two-phase: φ=0 reduces to single-phase Stokes</em></td><td style="text-align:right;"><code>permeability</code>, <code>inv_compaction_viscosity</code>, <code>melt_weakened_viscosity</code>, <code>compaction_length</code>, and the gate that <code>solve_twophase!</code> at zero porosity reproduces <code>picard_stokes!</code> velocities (see <code>doc/design/two_phase_flow.md</code> and <a href="./../physics/two_phase">Two-phase flow</a>).</td></tr></tbody></table><h3 id="Benchmarks-gated-by-RUN_BENCHMARKS" tabindex="-1">Benchmarks (gated by <code>RUN_BENCHMARKS</code>) <a class="header-anchor" href="#Benchmarks-gated-by-RUN_BENCHMARKS" aria-label="Permalink to &quot;Benchmarks (gated by `RUN_BENCHMARKS`) {#Benchmarks-gated-by-RUN_BENCHMARKS}&quot;">​</a></h3><p>The four short benchmark runs are real <code>run_case</code> invocations and take minutes, so <code>test/runtests.jl</code> includes them only when <code>ENV[&quot;RUN_BENCHMARKS&quot;] == &quot;1&quot;</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">File</th><th style="text-align:right;">What it runs / asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test/benchmarks/run_solcx.jl</code></td><td style="text-align:right;">SolCx (<code>cases/solcx.toml</code>) with the analytic <code>rho_field_solcx</code>/<code>eta_field_solcx</code> overrides; finite velocity DOFs and a bounded `</td></tr><tr><td style="text-align:right;"><code>test/benchmarks/run_falling_block.jl</code></td><td style="text-align:right;">Falling block, 50 steps; finite DOFs, nonzero sinking velocity.</td></tr><tr><td style="text-align:right;"><code>test/benchmarks/run_plume.jl</code></td><td style="text-align:right;">Plume, shortened to 20 steps via <code>clone_config</code>; finite DOFs.</td></tr><tr><td style="text-align:right;"><code>test/benchmarks/run_blankenbach.jl</code></td><td style="text-align:right;">Blankenbach 1A, 50 steps; <code>nusselt</code> and <code>vrms</code> finite (reference values Nu ≈ 4.884, Vrms ≈ 42.86 are logged, not asserted at this length).</td></tr></tbody></table><p>Quantitative pass/fail against reference values is the job of the longer studies below; the gated benchmarks only guard against regressions that break a full coupled run. See <a href="./../reference/benchmarks">Benchmarks</a> for the published comparisons.</p><h3 id="Longer-studies-run-manually,-not-part-of-the-suite" tabindex="-1">Longer studies (run manually, not part of the suite) <a class="header-anchor" href="#Longer-studies-run-manually,-not-part-of-the-suite" aria-label="Permalink to &quot;Longer studies (run manually, not part of the suite) {#Longer-studies-run-manually,-not-part-of-the-suite}&quot;">​</a></h3><p>These scripts <code>include(main.jl)</code> themselves and are launched standalone:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/validation/solcx_convergence.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # nx = 33/65/129 order estimate</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/validation/blankenbach_convergence.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # Nu, Vrms vs nx = 41/61/81</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/long_bench.jl</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                          # 4-case laptop-budget sweep (~30-45 min)</span></span></code></pre></div><p><code>test/performance/solver_2d.jl</code> (<em>Performance: MUMPS scaling (2D)</em>) is an informational scaling probe at nx = 21/41/81; it expects the <code>ROOT</code> constant and source graph from <code>runtests.jl</code>, so <code>include</code> it from a session that has already run the suite.</p><h2 id="MPI:-serialize-concurrent-Julia-runs" tabindex="-1">MPI: serialize concurrent Julia runs <a class="header-anchor" href="#MPI:-serialize-concurrent-Julia-runs" aria-label="Permalink to &quot;MPI: serialize concurrent Julia runs {#MPI:-serialize-concurrent-Julia-runs}&quot;">​</a></h2><p>Every MILET process — <code>main.jl</code>, <code>test/runtests.jl</code>, and the validation scripts alike — calls <code>MPI.Init()</code> at load time because MUMPS requires an MPI communicator (the binding is pinned to <code>MPICH_jll</code> in <code>LocalPreferences.toml</code>). The process is a single-rank MPI &quot;singleton&quot;; no <code>mpiexec</code> is involved.</p><p><strong>Do not launch multiple MILET Julia processes concurrently from the same working tree.</strong> Run the test suite, benchmarks, and validation scripts one at a time. Two concurrently initializing MPICH singletons on one machine can interfere with each other, and the failure mode is ugly rather than clean: MUMPS factorization handles perform <em>collective</em> MPI calls in their destructors, so a process whose MPI state has been disturbed (or torn down in the wrong order) dies via <code>abort()</code> with exit code 1 and its buffered stdout discarded — see the <code>_MUMPS_LIVE</code> registry and <code>finalize_mumps_contexts!</code> in <code>src/solvers/Mumps.jl</code>, and the matching comment at the entry point of <code>main.jl</code>. Concurrent runs of the same case would additionally race on the shared <code>output/&lt;case&gt;/</code> directories (checkpoints, PVD series, <code>run.log</code>).</p><p>If you need to compare configurations, run them sequentially in one session (the pattern used by <code>test/long_bench.jl</code>) or use distinct machines.</p><h2 id="Adding-a-test" tabindex="-1">Adding a test <a class="header-anchor" href="#Adding-a-test" aria-label="Permalink to &quot;Adding a test {#Adding-a-test}&quot;">​</a></h2><ol><li><p>Create <code>test/smoke/test_&lt;topic&gt;.jl</code>. Use <code>using Test</code> and one or more <code>@testset</code> blocks; everything from <code>src/</code> is already in scope.</p></li><li><p>Append an <code>include(joinpath(@__DIR__, &quot;smoke&quot;, &quot;test_&lt;topic&gt;.jl&quot;))</code> line to the smoke list in <code>test/runtests.jl</code> — files run in list order, so put it after any file whose helpers you reuse.</p></li><li><p>Conventions worth following:</p></li></ol><ul><li><p>Build configurations from a shipped case plus overrides, never by positional <code>Config</code> construction: <code>cfg = clone_config(load_config_toml(joinpath(ROOT, &quot;cases/falling_block.toml&quot;)); nx = 25, ny = 25, ntot = 1, ...)</code>. <code>clone_config</code> (<code>src/io/TOMLLoader.jl</code>) keeps tests immune to <code>Config</code> field growth.</p></li><li><p>Likewise build <code>RockProp</code>s with the keyword <code>_test_rock</code> helper from <code>test/smoke/test_rheology.jl</code> rather than positionally.</p></li><li><p>Keep smoke tests at toy resolutions (≤ 33² mesh, <code>ntot = 1</code>, <code>n_picard = 1</code>) — the suite should stay in the minutes range. Anything that needs a long run belongs behind the <code>RUN_BENCHMARKS</code> gate or in <code>test/validation/</code>.</p></li><li><p>Point any file output at a throwaway directory, e.g. <code>output_dir = &quot;output/_test_&lt;topic&gt;&quot;</code>.</p></li><li><p>Prefer analytic identities and limits (the φ = 0 two-phase gate, the grain-growth closed form) over golden numbers; when a reference value exists, log it with <code>@info</code> and assert a tolerance you can defend.</p></li></ul><ol start="4"><li>For a new gated benchmark, add the file under <code>test/benchmarks/</code> and include it inside the <code>if get(ENV, &quot;RUN_BENCHMARKS&quot;, &quot;0&quot;) == &quot;1&quot;</code> block of <code>test/runtests.jl</code>.</li></ol><p>Related: <a href="./architecture">Architecture</a> for the include graph the runner replicates, and the case files under <code>cases/</code> for the TOML definitions the tests load.</p>',26)])])}const p=t(a,[["render",n]]);export{u as __pageData,p as default};
